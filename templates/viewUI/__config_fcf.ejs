
      fcf: {
          <% if (connectionType == 'port') { %>          
            host: '<%= host %>',
            port: <%= port %>,<% } 
          else { %>          
            socketPath: '<%= socketPath %>',<% 
          } %>
          user: '<%= user %>',
          password: '<%= password %>',
          database: '<%= database %>',
          module:   'sails-mysql'
      }
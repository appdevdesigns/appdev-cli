
   // AppDev: your CAS instance config here:
  ,cas:{
      baseURL: "<%= baseURL %>",
      pgtURL : <%  if (pgtURL == 'none') { %>undefined<% } else { %>"<%= pgtURL %>"<% } %>,
      guidKey: "<%= guidKey || 'id' %>",
      sslCA: undefined
  }
  
};
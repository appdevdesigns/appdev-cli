
  // AppDev:  mysql keep your specific instance configurations here:
  ,connections:{
      appdev_default: {
<% if (connectionType == 'port') { %>          host: '<%= host %>',
          port: <%= port %>,<% } else { %>          socketPath: '<%= socketPath %>',<% } %>
          user: '<%= user %>',
          password: '<%- String(password).replace("'", "\'") %>',
          database: '<%= database %>'
      }
  }
  
};

   // AppDev: your Google OAuth2 credentials and config here:
  ,google:{
      baseURL: "<%= (baseURL == 'autodetect') ? '' : baseURL %>", // leave empty to use sails.getBaseurl()
      clientID : "<%= clientID %>",
      clientSecret: "<%= clientSecret %>"
  }
  
};